<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.iu.s1.board.qna.QnaDAO">
	
	<select id="detailFile" parameterType="QnaFileDTO" resultType="QnaFileDTO">
   		SELECT * FROM  QNAFILE
   		WHERE FILENUM = #{fileNum}
   	</select>
	
	<insert id="reply" parameterType="QnaDTO">
		 INSERT INTO QNA (NUM, TITLE, WRITER, CONTENTS,REGDATE,HIT, ref, step, depth)
    values (QNANUM_SEQ.NEXTVAL, #{title}, #{contents}, #{writer}, sysDate, 0, #{ref}, #{step}, #{depth})
	
	</insert>
	
	
	<update id="stepUpdate" parameterType="QnaDTO">
		update qna set step=step+1
		where ref=#{ref} and step>#{step}
	</update>
	
	<select id="list" parameterType="Pager" resultType="QnaDTO">
		SELECT * FROM
		    (SELECT rownum R, B.* FROM 
		        (SELECT * FROM QNA WHERE 
		        	NUM>0 
		        	and 
		        	
		        	<choose>
		        		<when test="kind=='col1'">
		        			TITLE
		        		</when>
		        		<when test="kind=='col2'">
		        			CONTENTS
		        		</when>
		        		<otherwise>
		        			WRITER
		        		</otherwise>
		        	</choose>
		        	
		        	 
		        	LIKE '%'||#{search}||'%'
		            ORDER BY REF DESC, STEP ASC
		         ) B
		     )
		WHERE R between #{startRow} and #{lastRow}
	</select>
	
	
	
	
	<select id="detail" parameterType="QnaDTO" resultType="QnaDTO" resultMap="detailResult">
		SELECT Q.*, QF.*
    	FROM	 QNA Q
    		LEFT OUTER JOIN
    		QNAFILE QF
    		ON Q.NUM=QF.NUM
    	WHERE Q.NUM= #{num}
	</select>
	
	<resultMap type="QnaDTO" id="detailResult">
		<id column="num" property="num"/>
		<result column="title" property="title"/>
		<result column="contents" property="contents"/>
		<result column="writer" property="writer"/>
		<result column="regDate" property="regDate"/>
		<result column="hit" property="hit"/>
		<result column="ref" property="ref"/>
		<result column="step" property="step"/>
		<result column="depth" property="depth"/>
		<collection property="fileDTOs" javaType="List" ofType="QnaFileDTO">
			<id column="fileNum" property="fileNum"/>
			<result column="fileName" property="fileName"/>
			<result column="oriName" property="oriName"/>
		</collection>
	
	</resultMap>
	
	
	<insert id="addFile" parameterType="BoardFileDTO">
		
		INSERT INTO QNAFILE (fileNUM, NUM, FILENAME,ORINAME)
		values (FILE_SEQ.NEXTVAL, #{num}, #{fileName}, #{oriName})
	</insert>
	
	<insert id="add" parameterType="QnaDTO">
		 <selectKey keyProperty="num" order="BEFORE" resultType="java.lang.Long">
		 SELECT QNANUM_SEQ.NEXTVAL FROM DUAL
		 </selectKey>
		 INSERT INTO QNA (NUM, TITLE, CONTENTS, WRITER,REGDATE,HIT, ref, step, depth)
    values (#{num}, #{title}, #{contents}, #{writer}, sysDate, 0, QNANUM_SEQ.currval, 0, 0)
	</insert>
	
	<delete id="delete" parameterType="QnaDTO">
			delete QNA where num = #{num}
			</delete>
			
		<select id="total" resultType="java.lang.Long" parameterType="Pager">
 		SELECT COUNT(NUM) FROM QNA
 		WHERE
 			NUM>0 
		    and 
		       	
		     <choose>
		        		<when test="kind=='col1'">
		        			TITLE
		        		</when>
		        		<when test="kind=='col2'">
		        			CONTENTS
		        		</when>
		        		<otherwise>
		        			WRITER
		        		</otherwise>
		        	</choose>
        	LIKE '%'||#{search}||'%' 
 	</select>	
 	
 	<update id="update" parameterType="QnaDTO">
 		update QNA set title=#{title}, contents=#{contents} where num=#{num}
 	</update>
 	
	</mapper>